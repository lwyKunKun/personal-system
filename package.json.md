# package.json 说明

此文件为项目的 npm 包信息与依赖声明。包含：

- `name`、`version`、`private`：项目信息
- `scripts`：常用命令（`dev`、`build`、`start`、`lint`）
- `dependencies`：运行时依赖（`next`、`react`、`react-dom`）
- `devDependencies`：开发依赖（TypeScript、Tailwind、ESLint 等）

如果需要 pin 特定依赖版本或添加构建相关插件，可在此更新。
