// next.config.ts
// 项目级 Next.js 配置文件
// 说明：
// - 在此文件中可配置 Next.js 行为（例如 rewrites、redirects、env、experimental 等）
// - 当前模板保持最小配置；如果需要自定义托管或构建行为，可在此扩展
import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  /* config options here */
}

export default nextConfig
